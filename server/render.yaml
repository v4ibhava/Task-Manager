services:
  - type: web
    name: task-manager-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        scope: run
        sync: false
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        scope: run
        sync: false
